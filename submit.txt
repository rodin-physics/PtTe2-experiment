#!/bin/bash
#PBS -P PtTe2_animations
#PBS -j oe
#PBS -N spectral_map
#PBS -q parallel24
#PBS -l select=1:ncpus=24:mem=48gb
#PBS -l walltime=01:00:00

cd $PBS_O_WORKDIR;
np=$(cat ${PBS_NODEFILE} | wc -l);

source /etc/profile.d/rec_modules.sh
module load julia/1.5.1
julia Calculation/spectral_map_smallgrid_anim.jl
